import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.println(" enter the  Principle amount : " +
                "\n enter the Time : " +
                "\n Enter the Rate % : ");
        int P = input.nextInt();
        int T = input.nextInt();
        int R = input.nextInt();

        int Total_intrest = P * T * R /100;

        System.out.println(" the simple intrest is : " + Total_intrest);



    }
}


import java.util.Scanner;

public class Manu {

    public static double fact(double x) {
        double f = 1;

        for (double i = 1; i <= x; i++) {
            f = f * i;
        }

        return f;
    }

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        double n = sc.nextInt();
        double r = sc.nextInt();

        double ncr = fact(n) / (fact(r) * fact(n - r));
        double npr = fact(n) / fact(n - r);

        System.out.print(ncr + " " + npr);

        sc.close();
    }
}


